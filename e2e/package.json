{
  "name": "e2e",
  "version": "1.0.0",
  "description": "End-to-end tests (Playwright) against the docker-compose stack (localhost:4200)",
  "private": true,
  "scripts": {
    "test": "playwright test"
  },
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
